import React from 'react'
import { Button } from "@/components/ui/button"

const ClockPage = () => {
    return (
        <div>
            <div className="text-4xl font-bold">Clock Page</div>
            <Button variant="destructive">Button</Button>
        </div>
    );
};

export default ClockPage;